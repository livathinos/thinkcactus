#search
  = form_tag url_for(:controller => params[:controller]), :method => :get do
    %ul
      %li= search_field_tag "search", nil, :id => "quicksearch", :title => (params[:search] || _t("Search"))
    - hidden_params.each do |field|
      = field
